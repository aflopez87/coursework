function nextBigger(n){
//   convert number to string
  const numStr = n.toString();
//   make string into an array of characters
  const stringArr = numStr.split(" ");
//   compare each character from right to left
  for (let i = 0; i < stringArr.length; i++) {}
//   take the greater character and move to the left while moving all other lesser characters to the right
//   combine characters into a string
//   convert string into an number
  
}